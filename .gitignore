# Secretos y datos — NUNCA subir
.env
certs/*.key
certs/*.crt
certs/*.csr
certs/*.pem

# Datos locales (facturas, cache del token, etc.)
data/*.json
data/*.xml
data/*.db
data/*.db.*
data/*.bak*
data/*.sqlite
data/*.sqlite3

# Python
__pycache__/
*.pyc
.venv/
venv/
!.gitkeep

# imágenes guía subidas desde el panel
data/onboarding/

# helper local de pruebas (homologación)
run_homo_local.py
